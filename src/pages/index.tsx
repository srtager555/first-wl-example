import styled from "styled-components";

const ContentContainer = styled.div`
  height: 1000px;
  width: 1000px;
  background-color: #fff;
`;

function Home() {
  return (
    <ContentContainer>
      <p>a</p>
    </ContentContainer>
  );
}

export default Home;
